{
	"name": "@ydb-platform/ydb-slo-action-int",
	"description": "The init action initializes SLO tests.",
	"type": "module",
	"scripts": {
		"bundle": "bun build ./src/main.ts ./src/post.ts --target node --format esm --sourcemap --banner '/* eslint-disable */' --outdir dist",
		"format": "bunx --bun prettier **/*.ts --write",
		"lint": "bunx --bun oxlint **/*.ts --fix --node-plugin --import-plugin --security-plugin --disable-react-plugin"
	},
	"lint-staged": {
		"*.ts": [
			"bun run lint",
			"bun run format",
			"bun run bundle"
		]
	}
}
